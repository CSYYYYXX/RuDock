[package]
name = "wb-mcp"
description = "MCP server adapter: exposes daemon capabilities as MCP tools. (Stub in M1)"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "wb-mcp"
path = "src/main.rs"

[dependencies]
serde_json = { workspace = true }
