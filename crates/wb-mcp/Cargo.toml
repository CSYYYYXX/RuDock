[package]
name = "wb-mcp"
description = "MCP stdio server exposing WB daemon tools and plugin Skills."
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "wb-mcp"
path = "src/main.rs"

[dependencies]
serde_json = { workspace = true }
wb-core = { path = "../wb-core" }
interprocess = "2"
