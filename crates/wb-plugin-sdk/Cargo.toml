[package]
name = "wb-plugin-sdk"
description = "Plugin development SDK: manifest types, command schemas, IPC client helpers."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
